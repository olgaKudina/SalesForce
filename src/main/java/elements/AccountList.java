package elements;
import org.openqa.selenium.WebDriver;
public class AccountList {
        WebDriver driver;
        String accountFields;
        private static final String ACCOUNTFIELS_XPATH = "//*[contains(@class,'slds-text-title')]";
}
